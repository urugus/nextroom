version: 3

files:
  - pattern: .github/workflows/*.yml
  - pattern: .github/workflows/*.yaml

min_age:
  value: 7
  always: true

rules:
  - min_age: 0
    conditions:
      - expr: |
          ActionRepoFullName == "suzuki-shunsuke/pinact-action"
